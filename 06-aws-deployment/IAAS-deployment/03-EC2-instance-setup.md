## EC2 Instance Setup

Four EC2 instances is setup to host the following services:
* **Tomcat service** 
* **MySQL service**
* **Rabbit MQ**
* **Memcache**

As these instances are deployed, custom sripts to setup the required services each instance would host is added to the deployment setup. 